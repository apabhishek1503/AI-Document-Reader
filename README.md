# AI-Document-Reader
An AI-powered document reader that intelligently analyzes, summarizes, and extracts key information from various file formats for quick and efficient understanding.
simple and interactive interface.
inside terminal write python -m venv venv
next type venv\scripts\activate
# Liberries 
pip install streamlit 
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install google-generativeai
pip install faiss-cpu
pip install pypdf
pip install python-docx
pip install senetence-transformers
pip install python-dotenv
